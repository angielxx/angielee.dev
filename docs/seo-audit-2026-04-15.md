# SEO 전체 감사 보고서 — angielee-dev.vercel.app

**감사 일자:** 2026-04-15
**프레임워크:** Next.js 16 App Router / Vercel
**도구:** Claude Code SEO Audit (7개 병렬 서브에이전트)

---

## SEO 종합 점수: 50 / 100

| 카테고리 | 가중치 | 점수 | 가중 점수 |
|--------|--------|------|---------|
| Technical SEO | 22% | 54 | 11.9 |
| 콘텐츠 품질 (E-E-A-T) | 23% | 62 | 14.3 |
| On-Page SEO | 20% | 45 | 9.0 |
| 구조화 데이터 (Schema) | 10% | 45 | 4.5 |
| 성능 (Core Web Vitals) | 10% | 55 | 5.5 |
| AI 검색 준비도 (GEO) | 10% | 34 | 3.4 |
| 이미지 | 5% | 25 | 1.3 |
| **합계** | | | **49.9** |

### 잘 된 것
- Next.js SSG로 완전한 HTML 전달 (크롤러 친화적)
- `lang="ko"` 설정
- `font-display: swap` 적용
- `next/image` + `sizes` 속성 사용
- `rel="noopener noreferrer"` 적용
- 회고/경험 콘텐츠 품질 높음 (항해플러스, 우아콘 포스트)
- Vercel 호스팅 (HTTP/2, Brotli, CDN, HTTPS)

### 주요 문제
- sitemap 없음, robots.txt 없음
- OG 이미지 파일(`/public/og-image.png`) 없음
- About 페이지 프로덕션에서 숨겨짐 (`isDev` 가드)
- `SITE_URL` 트레일링 슬래시 → `@id` 이중 슬래시 버그

---

## 우선순위별 액션 플랜

### Critical — 즉시 수정 필요

| # | 문제 | 파일 |
|---|------|------|
| C1 | **sitemap.xml 없음** — 32개 블로그 포스트를 크롤러가 링크 추적으로만 발견해야 함 | `app/sitemap.ts` 신규 생성 |
| C2 | **robots.txt 없음** — AI 크롤러(GPTBot, ClaudeBot, PerplexityBot)에 명시적 허용 불가 | `app/robots.ts` 신규 생성 |
| C3 | **`/public/og-image.png` 없음** — `layout.tsx`에서 참조하지만 파일 미존재, 모든 페이지 소셜 프리뷰 깨짐 | `public/og-image.png` (1200×630) 생성 |
| C4 | **`SITE_URL` 트레일링 슬래시** → `@id`에 `//` 이중 슬래시 버그 (`https://angielee-dev.vercel.app//#website`) | `lib/site.ts:1` |

### High — 1주 내 수정

| # | 문제 | 파일 |
|---|------|------|
| H1 | **About/Contact 페이지 프로덕션에서 숨겨짐** — `isDev` 가드로 내비게이션 링크 차단, 고아 페이지가 됨 | `components/Header.tsx:108`, `components/HeroSection.tsx:70` |
| H2 | **보안 헤더 없음** — `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, HSTS | `next.config.ts` |
| H3 | **블로그 포스트별 canonical 없음** — 루트 레이아웃의 canonical이 모든 포스트에 덮어씌워짐 | `app/blog/[slug]/page.tsx` `generateMetadata()` |
| H4 | **`/blog` 페이지 canonical 없음** — `?category=` 필터 URL이 중복 콘텐츠 신호 생성 | `app/blog/page.tsx` |
| H5 | **BlogPosting에 `url`, `mainEntityOfPage` 없음** — Google Article 리치 결과 자격 미달 | `app/blog/[slug]/page.tsx:73-85` |
| H6 | **BlogPosting author가 `#person` 그래프에 연결 안 됨** — 인라인 선언으로 지식 그래프 단절 | `app/blog/[slug]/page.tsx` |
| H7 | **`favicon.svg` 참조하지만 파일 없음** — `public/`에 미존재, 404 발생 | `app/layout.tsx:34` |
| H8 | **모든 이미지 alt 텍스트 비어 있음** — MDX 내 `![]()` 패턴 전체, AI 이미지 인덱싱 불가 | `posts/*.mdx` 전체 |
| H9 | **Pretendard CDN에 `preconnect` 없음** — LCP 300-600ms 지연 | `app/layout.tsx` |
| H10 | **Hero `<h1>` opacity:0으로 시작** — JS 하이드레이션 전까지 LCP 요소 불가시 | `components/HeroSection.tsx` |

### Medium — 1개월 내 수정

| # | 문제 | 파일 |
|---|------|------|
| M1 | **BlogPosting `dateModified`, `inLanguage`, `publisher` 없음** | `app/blog/[slug]/page.tsx` |
| M2 | **BreadcrumbList 스키마 없음** — 모든 페이지 | `app/blog/[slug]/page.tsx` 신규 추가 |
| M3 | **`/blog`, `/about`, `/contact`, `/` 에 페이지별 스키마 없음** | 각 `page.tsx` |
| M4 | **ThemeToggle SSR 불일치** — 초기 렌더에 잘못된 아이콘 표시, CLS 유발 | `components/ThemeToggle.tsx` |
| M5 | **PostCard `<time>` 에 `dateTime` 속성 없음** | `components/PostCard.tsx:124` |
| M6 | **framer-motion LazyMotion 미사용** — 모든 페이지에 전체 번들 로드, INP 저하 | 모든 `motion` 사용 컴포넌트 |
| M7 | **`llms.txt` 없음** — ChatGPT/Perplexity/Claude 인용 신호 미생성 | `public/llms.txt` |
| M8 | **H1이 키워드 없이 이름만** — "Angie Lee" 단독, 주제 관련성 신호 제로 | `components/HeroSection.tsx` |
| M9 | **2023년 Next.js 13 포스트 갱신 표기 없음** — App Router 기준으로 구식 정보 | `posts/01-what_is_nextjs_and_why_use.mdx` |
| M10 | **debounce_in_React.mdx 내 이전 도메인 링크** — `eun-jee.com`으로 깨진 링크 | `posts/debounce_in_React.mdx:9` |
| M11 | **32개 포스트 중 19개 `tags: []`** — 태그 필터 및 토픽 클러스터 신호 없음 | `posts/*.mdx` 다수 |
| M12 | **`/blog` 페이지 OG/Twitter 태그 없음** — 홈과 동일한 소셜 프리뷰 | `app/blog/page.tsx` |
| M13 | **포스트별 썸네일 없을 때 OG 이미지 폴백 없음** | `app/blog/[slug]/page.tsx` |
| M14 | **Person 스키마 빈약** — `jobTitle`, `knowsAbout` 없어 엔티티 인식 어려움 | `app/layout.tsx` |

### Low — 백로그

- `CategoryFilter`에서 `router.replace` + `useTransition` 사용 (히스토리 오염 방지)
- Pretendard 폰트 `next/font/local`로 셀프 호스팅 전환
- 시리즈 frontmatter 미사용인 debounce 3부작에 `series`/`seriesOrder` 추가
- IndexNow 프로토콜 구현 (네이버/Bing용)
- 태그 전용 라우트 (`/blog/tag/[tag]`) 고려
- `frontmatter`에 `lastReviewed` 필드 추가

---

## 권장 구현 순서

```
주 1 (Critical):
  1. lib/site.ts — 트레일링 슬래시 제거
  2. app/robots.ts — 생성
  3. app/sitemap.ts — 생성
  4. public/og-image.png — 1200×630 생성

주 2 (High):
  5. next.config.ts — 보안 헤더 추가
  6. app/layout.tsx — preconnect 추가, favicon 수정
  7. app/blog/[slug]/page.tsx — canonical, BlogPosting 스키마 완성
  8. components/Header.tsx + HeroSection.tsx — isDev 가드 제거
  9. posts/*.mdx — alt 텍스트 일괄 추가

주 3-4 (Medium):
  10. BreadcrumbList, 페이지별 스키마 추가
  11. ThemeToggle SSR 수정
  12. llms.txt 생성
  13. 태그 일괄 추가, 깨진 링크 수정
```

---

## 카테고리별 상세 분석

### Technical SEO (54/100)

- 크롤러빌리티: 0/10 — sitemap, robots.txt 모두 없음
- 인덱서빌리티: 6/10 — canonical 미설정
- 보안: 4/10 — 보안 헤더 없음
- URL 구조: 6/10 — 슬러그 기반 정적 라우팅은 양호
- 모바일: 8/10 — 반응형 레이아웃, viewport 메타 정상
- CWV 준비도: 6/10 — SSG 완전 구현이나 LCP 리스크 존재
- 구조화 데이터: 7/10 — JSON-LD 존재하나 버그 및 누락 필드
- JS 렌더링: 9/10 — generateStaticParams() 완전 구현
- IndexNow: 0/10 — 미구현

### 콘텐츠 품질 E-E-A-T (62/100)

- Experience: 72/100 — 회고/경험 포스트 강함, 개념 포스트 약함
- Expertise: 58/100 — 기술 포스트 표면적 요약 수준, 출처 없음
- Authoritativeness: 38/100 — About 페이지 숨김, 외부 권위 신호 전무
- Trustworthiness: 61/100 — HTTPS, 이메일 노출, 깨진 링크 존재

### Schema 구조화 데이터 (45/100)

주요 버그:
- `SITE_URL` 트레일링 슬래시 → 모든 `@id` 이중 슬래시
- `BlogPosting`에 `url`, `mainEntityOfPage` 없음
- Author 인라인 선언으로 `#person` 노드 연결 끊김
- BreadcrumbList 없음
- `/blog`, `/about`, `/contact` 페이지 스키마 없음

### 성능 Core Web Vitals (55/100)

- **LCP 리스크:** Pretendard CDN preconnect 없음 (+300-600ms), Hero H1 opacity:0 (+200-500ms)
- **INP 리스크:** framer-motion 전체 번들, Header 전체 클라이언트 컴포넌트
- **CLS:** ThemeToggle 초기 아이콘 플래시, framer-motion header 입장 애니메이션
- **긍정:** SSG 완전 구현, aspect-video 썸네일, font-display:swap

### AI 검색 준비도 GEO (34/100)

- **기술 접근성 (45/100):** robots.txt 없음, sitemap 없음, SSR/SSG는 우수
- **권위/브랜드 신호 (18/100):** sameAs GitHub 하나뿐, Person 엔티티 약함
- **인용 가능성 (32/100):** 직접 답변 구조 없음, 질문형 헤딩 부족
- **구조적 가독성 (45/100):** 이중 H1 문제, TOC 구조는 양호
- **멀티모달 (30/100):** 이미지 alt 텍스트 전무, OG 이미지 파일 없음
